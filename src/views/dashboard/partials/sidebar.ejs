<%# Customer Dashboard Sidebar Partial %>
<%
const userNameVal = typeof userName !== 'undefined' ? userName : 'Customer';
const activePage = typeof dashboardPage !== 'undefined' ? dashboardPage : '';
const brandName = typeof storeName !== 'undefined' && storeName ? storeName : 'OpenUMKM';
const brandInitial = (brandName.trim()[0] || 'O').toUpperCase();
const brandRest = brandName.trim().slice(1);
%>
<aside class="dashboard-sidebar" aria-label="Dashboard navigation">
  <div class="dashboard-sidebar-logo">
    <span><%= brandInitial %></span><%= brandRest %>
  </div>

  <div class="flex items-center gap-3" style="padding: var(--space-3); background: var(--color-gray-50); border-radius: var(--radius-md); margin-bottom: var(--space-6);">
    <div class="avatar" style="width: 44px; height: 44px; font-size: 16px;" aria-hidden="true"><%= userNameVal.charAt(0).toUpperCase() %></div>
    <div>
      <p style="font: var(--font-label-sm); color: var(--color-text); margin-bottom: 0;"><%= userNameVal %></p>
      <p style="font: var(--font-label-xs); color: var(--color-text-muted);"><%= t('dashboard.customer', 'Customer') %></p>
    </div>
  </div>

  <nav class="sidebar-nav" aria-label="Dashboard menu">
    <a href="/dashboard" class="sidebar-nav-item<%= activePage === 'dashboard' ? ' active' : '' %>">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
      <%= t('dashboard.my_dashboard') %>
    </a>
    <a href="/dashboard/orders" class="sidebar-nav-item<%= activePage === 'orders' ? ' active' : '' %>">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
      <%= t('dashboard.my_orders') %>
    </a>
    <a href="/dashboard/addresses" class="sidebar-nav-item<%= activePage === 'addresses' ? ' active' : '' %>">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
      <%= t('dashboard.addresses') %>
    </a>
  </nav>

  <div class="sidebar-footer">
    <a href="/" class="sidebar-nav-item">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
      <%= t('storefront.back_to_store') %>
    </a>
    <a href="/auth/logout" class="sidebar-nav-item" style="color: var(--color-danger);">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
      <%= t('storefront.logout') %>
    </a>
  </div>
</aside>
