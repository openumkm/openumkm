<% const userNameVal = typeof userName !== 'undefined' ? userName : 'Customer'; %>

<%- include('../partials/head', { pageTitle: typeof pageTitle !== 'undefined' ? pageTitle : 'Dashboard' }) %>

<body>
<div class="dashboard-layout">
  <aside class="dashboard-sidebar" aria-label="Dashboard navigation">
    <div class="dashboard-sidebar-logo">
      <span>S</span>wift Commerce
    </div>

    <div class="flex items-center gap-3" style="padding: var(--space-3); background: var(--color-gray-50); border-radius: var(--radius-md); margin-bottom: var(--space-6);">
      <div class="avatar" style="width: 44px; height: 44px; font-size: 16px;" aria-hidden="true"><%= userNameVal.charAt(0).toUpperCase() %></div>
      <div>
        <p style="font: var(--font-label-sm); color: var(--color-text); margin-bottom: 0;"><%= userNameVal %></p>
        <p style="font: var(--font-label-xs); color: var(--color-text-muted);">Customer</p>
      </div>
    </div>

    <nav class="sidebar-nav" aria-label="Dashboard menu">
      <a href="/dashboard" class="sidebar-nav-item active">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
        Dashboard
      </a>
      <a href="/dashboard/orders" class="sidebar-nav-item">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
        My Orders
      </a>
      <a href="/dashboard/addresses" class="sidebar-nav-item">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
        Addresses
      </a>
    </nav>

    <div class="sidebar-footer">
      <a href="/auth/logout" class="sidebar-nav-item" style="color: var(--color-danger);">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
        Logout
      </a>
    </div>
  </aside>

  <main class="dashboard-main" id="dashboardMain">
    <div class="dashboard-header">
      <div>
        <h2>Welcome back, <%= userNameVal %></h2>
        <p style="color: var(--color-text-secondary); margin-bottom: 0;">Here's what's happening with your orders.</p>
      </div>
    </div>

    <div class="stat-cards">
      <div class="stat-card">
        <div class="stat-card-icon primary">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
        </div>
        <div class="stat-card-label">Total Orders</div>
        <div class="stat-card-value"><%= typeof stats !== 'undefined' && stats ? stats.totalOrders || 0 : 0 %></div>
      </div>
      <div class="stat-card">
        <div class="stat-card-icon warning">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
        </div>
        <div class="stat-card-label">Pending</div>
        <div class="stat-card-value"><%= typeof stats !== 'undefined' && stats ? stats.pendingOrders || 0 : 0 %></div>
      </div>
      <div class="stat-card">
        <div class="stat-card-icon success">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
        </div>
        <div class="stat-card-label">Completed</div>
        <div class="stat-card-value"><%= typeof stats !== 'undefined' && stats ? stats.completedOrders || 0 : 0 %></div>
      </div>
    </div>

    <div class="dashboard-table-wrap">
      <div class="dashboard-table-header">
        <h3>Recent Orders</h3>
      </div>
      <% if (typeof recentOrders !== 'undefined' && recentOrders && recentOrders.length > 0) { %>
      <table class="data-table">
        <thead>
          <tr>
            <th>Order</th>
            <th>Date</th>
            <th>Status</th>
            <th>Total</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% recentOrders.forEach(function(order) { %>
          <tr>
            <td><strong>#<%= order.orderNumber || order.id %></strong></td>
            <td><%= order.createdAt ? new Date(order.createdAt).toLocaleDateString('en-ID', { day: 'numeric', month: 'short', year: 'numeric' }) : '—' %></td>
            <td>
              <span class="status <%= 'status-' + ((order.status || '').toLowerCase()) %>"><%= order.status || 'Pending' %></span>
            </td>
            <td class="amount">Rp <%= order.total ? order.total.toLocaleString('id-ID') : '0' %></td>
            <td><a href="/dashboard/orders/<%= order.id %>" style="color: var(--color-primary); font: var(--font-label-sm); font-weight: 600;">View</a></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      <% } else { %>
      <div class="empty-state">
        <div class="empty-state-icon">
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
        </div>
        <h3>No orders yet</h3>
        <p>When you place an order, it will appear here.</p>
      </div>
      <% } %>
    </div>
  </main>
</div>
</body>
</html>
