<% const userNameVal = typeof userName !== 'undefined' ? userName : 'Customer'; %>
<%- include('../partials/head', { pageTitle: typeof pageTitle !== 'undefined' ? pageTitle : t('dashboard.order_detail') }) %>
<body>
<div class="dashboard-layout">
  <%- include('./partials/sidebar', { userName: userNameVal, dashboardPage: 'orders' }) %>

  <main class="dashboard-main">
    <div class="dashboard-header">
      <div class="flex items-center gap-3">
        <h2><%= t('admin.order', 'Order') %> #<%= order.orderNumber || order.id %></h2>
        <span class="status <%= 'status-' + ((order.status || '').toLowerCase()) %>"><%= t('admin.' + (order.status || 'pending'), order.status || 'Pending') %></span>
      </div>
      <a href="/dashboard/orders" style="color: var(--color-primary); font: var(--font-label-sm); font-weight: 600;">&larr; <%= t('dashboard.back_to_orders', 'Back to Orders') %></a>
    </div>

    <div class="grid" style="margin-bottom: var(--space-6);">
      <div class="col-6" style="background: var(--color-surface); border: 1px solid var(--color-border-light); border-radius: var(--radius-lg); padding: var(--space-6);">
        <h4 style="margin-bottom: var(--space-4);"><%= t('admin.order_info', 'Order Information') %></h4>
        <div class="flex flex-col gap-2">
          <div class="flex justify-between" style="font: var(--font-body-md);">
            <span style="color: var(--color-text-muted);"><%= t('dashboard.date') %></span>
            <span><%= order.createdAt ? new Date(order.createdAt).toLocaleDateString(currentLang === 'id' ? 'id-ID' : 'en-US', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' }) : '—' %></span>
          </div>
          <div class="flex justify-between" style="font: var(--font-body-md);">
            <span style="color: var(--color-text-muted);"><%= t('dashboard.payment_method') %></span>
            <span><%= order.paymentMethod || '—' %></span>
          </div>
          <div class="flex justify-between" style="font: var(--font-body-md);">
            <span style="color: var(--color-text-muted);"><%= t('dashboard.courier') %></span>
            <span><%= order.courier || '—' %></span>
          </div>
        </div>
      </div>
    </div>

    <div class="dashboard-table-wrap" style="margin-bottom: var(--space-6);">
      <div class="dashboard-table-header">
        <h3><%= t('admin.items', 'Items') %></h3>
      </div>
      <table class="data-table">
        <thead>
          <tr>
            <th><%= t('admin.product', 'Product') %></th>
            <th><%= t('admin.variant', 'Variant') %></th>
            <th><%= t('admin.qty', 'Qty') %></th>
            <th><%= t('admin.product_price') %></th>
            <th><%= t('admin.subtotal', 'Subtotal') %></th>
          </tr>
        </thead>
        <tbody>
          <% if (order.items && order.items.length > 0) {
            order.items.forEach(function(item) { %>
          <tr>
            <td>
              <div class="flex items-center gap-3">
                <% if (item.image) { %>
                <img src="<%= item.image %>" alt="<%= item.name %>" style="width: 48px; height: 48px; border-radius: var(--radius-md); object-fit: cover;">
                <% } else { %>
                <div style="width: 48px; height: 48px; border-radius: var(--radius-md); background: var(--color-gray-100); display: flex; align-items: center; justify-content: center; color: var(--color-text-muted);">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
                </div>
                <% } %>
                <span><%= item.name %></span>
              </div>
            </td>
            <td><%= item.variant || '—' %></td>
            <td><%= item.quantity || 1 %></td>
            <td>Rp <%= item.price ? item.price.toLocaleString('id-ID') : '0' %></td>
            <td class="amount">Rp <%= item.subtotal ? item.subtotal.toLocaleString('id-ID') : ((item.price || 0) * (item.quantity || 1)).toLocaleString('id-ID') %></td>
          </tr>
          <% });
          } else { %>
          <tr>
            <td colspan="5" style="text-align: center; color: var(--color-text-muted); padding: var(--space-8);"><%= t('dashboard.no_items', 'No items found.') %></td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <div class="grid" style="margin-bottom: var(--space-6);">
      <div class="col-6">
        <div style="background: var(--color-surface); border: 1px solid var(--color-border-light); border-radius: var(--radius-lg); padding: var(--space-6);">
          <h4 style="margin-bottom: var(--space-4);"><%= t('dashboard.order_totals', 'Order Totals') %></h4>
          <div class="flex flex-col gap-2">
            <div class="flex justify-between" style="font: var(--font-body-md);">
              <span style="color: var(--color-text-muted);"><%= t('dashboard.subtotal') %></span>
              <span>Rp <%= order.subtotal ? order.subtotal.toLocaleString('id-ID') : (order.total || 0).toLocaleString('id-ID') %></span>
            </div>
            <div class="flex justify-between" style="font: var(--font-body-md);">
              <span style="color: var(--color-text-muted);"><%= t('dashboard.shipping') %></span>
              <span>Rp <%= order.shippingCost ? order.shippingCost.toLocaleString('id-ID') : '0' %></span>
            </div>
            <div class="flex justify-between" style="font: var(--font-body-md);">
              <span style="color: var(--color-text-muted);"><%= t('dashboard.tax') %></span>
              <span>Rp <%= order.tax ? order.tax.toLocaleString('id-ID') : '0' %></span>
            </div>
            <div style="border-top: 1px solid var(--color-border-light); padding-top: var(--space-3);" class="flex justify-between">
              <strong><%= t('dashboard.grand_total') %></strong>
              <strong style="font-size: 18px; color: var(--color-primary);">Rp <%= order.total ? order.total.toLocaleString('id-ID') : '0' %></strong>
            </div>
          </div>
        </div>
      </div>
      <div class="col-6">
        <% if (order.shippingAddress) { %>
        <div style="background: var(--color-surface); border: 1px solid var(--color-border-light); border-radius: var(--radius-lg); padding: var(--space-6); margin-bottom: var(--space-6);">
          <h4 style="margin-bottom: var(--space-3);"><%= t('dashboard.shipping_address') %></h4>
          <div style="font: var(--font-body-md); color: var(--color-text-secondary);">
            <p style="font-weight: 600; color: var(--color-text); margin-bottom: var(--space-1);">
              <%= order.shippingAddress.name || order.shippingAddress.label || '—' %>
            </p>
            <p style="margin-bottom: var(--space-1);"><%= order.shippingAddress.phone || '' %></p>
            <p><%= order.shippingAddress.address || '' %></p>
            <p><%= order.shippingAddress.city || '' %><%= order.shippingAddress.postalCode ? ', ' + order.shippingAddress.postalCode : '' %></p>
          </div>
        </div>
        <% } %>

        <% if (order.trackingNumber) { %>
        <div style="background: var(--color-surface); border: 1px solid var(--color-border-light); border-radius: var(--radius-lg); padding: var(--space-6);">
          <h4 style="margin-bottom: var(--space-3);"><%= t('dashboard.tracking_info', 'Tracking Information') %></h4>
          <div style="font: var(--font-body-md);">
            <div class="flex justify-between" style="margin-bottom: var(--space-2);">
              <span style="color: var(--color-text-muted);"><%= t('dashboard.tracking_number') %></span>
              <span style="font-weight: 600;"><%= order.trackingNumber %></span>
            </div>
            <% if (order.trackingUrl) { %>
            <a href="<%= order.trackingUrl %>" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-sm" style="margin-top: var(--space-2);"><%= t('dashboard.track_package', 'Track Package') %></a>
            <% } %>
          </div>
        </div>
        <% } %>
      </div>
    </div>

    <% if (order.paymentMethod === 'Manual Transfer' && (order.status === 'pending' || order.status === 'waiting_confirmation')) { %>
    <div style="background: var(--color-warning-bg); border: 1px solid #FDE68A; border-radius: var(--radius-lg); padding: var(--space-6); margin-bottom: var(--space-6);">
      <div class="flex items-center justify-between">
        <div>
          <h4 style="margin-bottom: var(--space-1);"><%= t('dashboard.payment_confirmation', 'Payment Confirmation') %></h4>
          <p style="color: var(--color-text-secondary); margin-bottom: 0;"><%= t('dashboard.payment_confirm_hint', 'Your payment is pending. Confirm your payment to process this order.') %></p>
        </div>
        <a href="/payment/confirm/<%= order.id %>" class="btn btn-primary"><%= t('dashboard.confirm_payment', 'Confirm Payment') %></a>
      </div>
    </div>
    <% } %>

    <a href="/dashboard/orders" style="display: inline-flex; align-items: center; gap: var(--space-2); color: var(--color-text-secondary); font: var(--font-label-sm); margin-top: var(--space-4);">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
      <%= t('dashboard.back_to_orders', 'Back to Orders') %>
    </a>
  </main>
</div>
</body>
</html>
